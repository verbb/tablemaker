// Plugin Kit web components the Table Maker field relies on. The register bundle
// defines these before the app entry queries them.
export const TABLEMAKER_PK_COMPONENTS = [
    'pk-icon',
    'pk-button',
    'pk-editable-table',
    'pk-dialog',
] as const;
